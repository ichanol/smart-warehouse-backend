const fullFormatDate = require("./fullFormatDate");
const shortFormatDate = require("./shortFormatDate");
const capitalize = require("./capitalize");

module.exports = {
  fullFormatDate,
  shortFormatDate,
  capitalize,
};
